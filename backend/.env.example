# This is an example environment configuration file (.env.example) for the backend.
# Copy this file to a new file named `.env` and replace the placeholder values with your actual configuration.

SECRET_KEY=django-insecure-dev-key
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# Email credentials used for SMTP (Gmail example)
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-email-app-password



# Each line should follow the format: VARIABLE_NAME=value
# Lines starting with '#' are comments and will be ignored.
# Use this file as a template to ensure all required environment variables are set for the application.
#DATABASE_URL=postgres://mshosen2001:12345@127.0.0.1:5432/kgsw
