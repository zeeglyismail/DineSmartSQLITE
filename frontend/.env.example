# Example environment variables for the frontend (Vite)
# Copy this file to .env and restart the dev server (vite) after editing.

# Base API URL for backend. Vite requires env vars to be prefixed with VITE_.
VITE_BACKEND_BASE_API=http://localhost:8000
